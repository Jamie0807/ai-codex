interface:
  display_name: "Quality Gate"
  short_description: "Run existing lint, tests, and build checks"
  default_prompt: "Use $quality-gate to run the existing project validation checks and summarize failures."
