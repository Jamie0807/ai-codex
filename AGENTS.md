# to-do App

## 技术栈

- React19, vite, shadcn-ui

## UI 设计语言

两个流派

1. 规范 md 约束AGENTS.md 约束
2. 设计稿转代码

## 项目结构与模块组织

当前仓库还是空工作区。后续添加代码时，请保持目录结构清晰、层级适中：

- `src/`：应用或库的源代码。
- `tests/`：覆盖公开行为的自动化测试。
- `docs/`：项目说明、架构决策和用户文档。
- `scripts/`：可重复执行的开发或 CI 脚本。
- `assets/`：静态资源，例如图片、测试夹具或示例数据。

项目扩大后，建议在 `src/` 中按功能组织模块。测试文件命名应尽量对应被测代码，便于定位。

## 构建、测试与开发命令

当前尚未配置构建系统。引入工具链后，请在此记录标准命令并保持稳定。推荐模式如下：

- `npm test`、`cargo test`、`pytest` 或同类命令：运行完整测试套件。
- `npm run build`、`cargo build` 或同类命令：生成生产可用产物。
- `npm run dev` 或同类命令：启动本地开发服务或监听器。
- `npm run lint` / `npm run format`：检查并统一代码风格。

避免依赖一次性的手动步骤。若需要初始化或配置，请写成脚本并记录命令。

## 编码风格与命名约定

确定技术栈后，优先使用该生态的格式化工具和 lint 工具。在工具链确定前，请保持代码可读且一致：

- 使用明确的文件和模块名，例如 `user_service.ts` 或 `user-service.test.ts`。
- 函数应职责单一，并用行为命名。
- 优先使用显式配置文件，不依赖隐藏的编辑器本地设置。
- 除非发布所必需，不提交生成产物。

## 测试规范

添加新行为时应同步添加测试。测试名称应描述期望行为，使失败信息易于理解，例如 `parses_valid_config` 或 `renders_empty_state`。共享夹具可放在 `tests/fixtures/` 或 `assets/fixtures/`。

## 提交与 Pull Request 规范

当前工作区没有 Git 历史，因此尚无仓库专属提交规范。建议使用简洁的祈使句提交信息，例如 `Add config parser` 或 `Fix test fixture loading`。

Pull Request 应包含变更摘要、变更原因、测试结果，以及相关 issue 链接。涉及 UI 时请附截图；涉及迁移或配置变更时需明确说明。

## Agent 专用说明

编辑前先检查当前目录树，并保留与任务无关的用户改动。变更应保持聚焦；一旦仓库形成既有约定，应优先遵循。新增工具链或目录结构后，请同步更新本文档。
